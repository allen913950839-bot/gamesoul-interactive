# 🚀 GameSoul - 部署指南

## 📋 部署前准备

### 1. 环境要求
- Node.js >= 16.x
- npm >= 8.x
- Git
- GitHub账号
- Vercel账号（免费）

### 2. 项目检查
```bash
# 确保代码可以正常构建
npm run build

# 检查是否有错误
npm run dev
```

---

## 🎯 方式一：GitHub + Vercel（推荐）

### 步骤1：推送到GitHub

如果还没有GitHub仓库：

```bash
# 1. 在GitHub创建仓库
# 访问：https://github.com/new
# 仓库名：gamesoul-interactive
# 类型：Public或Private
# 不要勾选任何初始化选项

# 2. 关联并推送
git remote add origin https://github.com/YOUR_USERNAME/gamesoul-interactive.git
git branch -M main
git add .
git commit -m "Initial commit"
git push -u origin main
```

如果已有仓库：
```bash
git add .
git commit -m "Update project"
git push origin main
```

### 步骤2：连接Vercel

1. **访问Vercel**：https://vercel.com
2. **使用GitHub登录**
3. **导入项目**：
   - 点击 "Add New..." → "Project"
   - 选择 `gamesoul-interactive` 仓库
   - 点击 "Import"

### 步骤3：配置项目

**构建配置**（保持默认）：
```
Framework Preset: Vite
Root Directory: ./
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

**环境变量**（重要！）：

展开 "Environment Variables"，添加：

```env
# Google Gemini API（必需，用于亚瑟对话）
VITE_GEMINI_API_KEY=your_gemini_api_key

# DeepSeek API（可选，用于光子鸡对话）
DEEPSEEK_API_KEY=your_deepseek_api_key

# Vercel KV 数据库（可选，用于保存功能）
KV_REST_API_URL=your_kv_url
KV_REST_API_TOKEN=your_kv_token
```

**环境选择**：✅ Production ✅ Preview ✅ Development

### 步骤4：部署

1. 点击 "Deploy" 按钮
2. 等待 1-2 分钟
3. 部署成功后获得链接：`https://your-project.vercel.app`

---

## 🗄️ 配置Vercel KV数据库（可选）

### 为什么需要？
保存对话、历史记录、广场功能需要数据库。

### 创建步骤

1. **在Vercel项目中创建KV**：
   - 进入项目 → Storage → Create Database
   - 选择 "KV"
   - 命名：`gamesoul-kv`
   - Region: 选择离你最近的
   - 点击 "Create"

2. **自动添加环境变量**：
   - Vercel会自动添加以下环境变量：
     - `KV_REST_API_URL`
     - `KV_REST_API_TOKEN`
     - `KV_URL`

3. **重新部署**：
   - Settings → Deployments
   - 点击最新部署右侧的 "..." → "Redeploy"

### 验证数据库

访问：`https://your-project.vercel.app/api/check-db`

应该看到：
```json
{
  "kvStatus": "connected",
  "message": "Vercel KV configured correctly"
}
```

---

## 🔄 自动部署流程

配置完成后，每次代码更新会自动部署：

```bash
# 1. 修改代码...

# 2. 提交并推送
git add .
git commit -m "描述你的修改"
git push origin main

# 3. Vercel自动检测并部署（1-2分钟）
```

**或使用快捷脚本**：
```bash
./update.sh
```

---

## 🌐 自定义域名（可选）

### 添加域名

1. **在Vercel项目中**：
   - Settings → Domains
   - 输入你的域名：`yourdomain.com`
   - 点击 "Add"

2. **配置DNS**：
   - Vercel会提供DNS记录
   - 在域名注册商处添加记录
   - 等待DNS生效（几分钟到几小时）

3. **自动HTTPS**：
   - Vercel自动配置SSL证书
   - 支持HTTP/2和HTTP/3

---

## 📊 监控与分析

### Vercel仪表板功能

访问：https://vercel.com/dashboard

- **Deployments**：查看所有部署历史
- **Analytics**：访问量、性能数据
- **Logs**：实时日志、错误追踪
- **Settings**：环境变量、域名管理

### 查看日志

```bash
# 查看构建日志
在Vercel Dashboard → Deployments → 点击某个部署 → Build Logs

# 查看运行时日志
在Vercel Dashboard → Deployments → 点击某个部署 → Function Logs
```

---

## 🔧 API密钥获取

### Google Gemini API

1. 访问：https://makersuite.google.com/app/apikey
2. 登录Google账号
3. 点击 "Create API Key"
4. 复制密钥

**免费额度**：每天60次请求

### DeepSeek API

1. 访问：https://platform.deepseek.com
2. 注册/登录账号
3. API Keys → Create API Key
4. 复制密钥

**免费额度**：新用户赠送500万tokens

---

## 🐛 常见问题

### Q: 推送代码失败，要求输入密码
**A**: GitHub已不支持密码认证，需要使用Personal Access Token：

1. 访问：https://github.com/settings/tokens
2. Generate new token (classic)
3. 勾选权限：`repo`
4. 复制token
5. 使用token作为密码，或配置：
```bash
git remote set-url origin https://YOUR_TOKEN@github.com/YOUR_USERNAME/gamesoul-interactive.git
```

### Q: Vercel部署成功但页面404
**A**: 检查：
1. Root Directory设置为 `./`（不是`/`）
2. Output Directory设置为 `dist`
3. 重新部署

### Q: API不工作，控制台报错
**A**: 检查：
1. Vercel环境变量是否正确添加
2. 变量名必须完全匹配（大小写敏感）
3. 前端变量必须以`VITE_`开头
4. 修改环境变量后需要重新部署

### Q: 保存功能失败
**A**: 
1. 检查是否创建了Vercel KV数据库
2. 访问 `/api/check-db` 验证连接
3. 查看Function Logs中的错误信息

### Q: 修改代码后网站没更新
**A**: 
1. 确认代码已推送到GitHub
2. 检查Vercel Deployments列表
3. 清除浏览器缓存：Ctrl+Shift+R（Win）或Cmd+Shift+R（Mac）

---

## ⚡ 性能优化建议

### 1. 启用预渲染
```javascript
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'framer': ['framer-motion']
        }
      }
    }
  }
}
```

### 2. 图片优化
- 使用WebP格式
- 压缩图片大小
- 使用CDN加速

### 3. 代码分割
- 已自动启用
- 按需加载组件

---

## 🔐 安全建议

### 1. 保护API密钥
- ❌ 不要在代码中硬编码
- ✅ 使用环境变量
- ✅ 不要提交`.env`文件到Git

### 2. 限制API调用
```javascript
// 添加请求频率限制
const rateLimit = {
  max: 10, // 每分钟最多10次
  windowMs: 60000
};
```

### 3. 验证用户输入
```javascript
// 过滤敏感内容
const sanitizeInput = (text) => {
  return text.replace(/<script>/gi, '');
};
```

---

## 📱 多环境部署

### 开发环境
```bash
npm run dev
# http://localhost:5173
```

### 预览环境
```bash
# 创建预览分支
git checkout -b preview/new-feature
git push origin preview/new-feature

# Vercel自动创建预览链接
# https://gamesoul-interactive-xxx.vercel.app
```

### 生产环境
```bash
# 合并到main分支
git checkout main
git merge preview/new-feature
git push origin main

# 自动部署到生产环境
# https://gamesoul-interactive.vercel.app
```

---

## 🎯 快速命令参考

```bash
# 本地开发
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 代码检查
npm run lint

# 快速更新并部署
./update.sh

# 查看Git状态
git status

# 提交代码
git add .
git commit -m "your message"
git push origin main
```

---

## 📞 获取帮助

- **Vercel文档**：https://vercel.com/docs
- **GitHub文档**：https://docs.github.com
- **Gemini API文档**：https://ai.google.dev/docs
- **DeepSeek文档**：https://platform.deepseek.com/docs

---

## 🎉 部署成功检查清单

- [ ] GitHub仓库创建成功
- [ ] 代码推送到GitHub
- [ ] Vercel项目连接成功
- [ ] 环境变量配置完成
- [ ] 部署成功，获得链接
- [ ] 访问链接，页面正常显示
- [ ] AI对话功能正常工作
- [ ] （可选）KV数据库配置完成
- [ ] （可选）自定义域名配置完成

---

**部署完成后，记得分享给朋友体验！** 🚀

*更新时间：2025-11-21*
